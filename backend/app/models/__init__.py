"""Pydantic content models (distinct from `app.db.models` SQLAlchemy rows).

Currently contains the expedition content schema; future content types
(missions, seasons, sanctuary regions) will land here too.
"""
