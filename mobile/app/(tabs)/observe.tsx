import PlaceholderScreen from "@/components/PlaceholderScreen";

export default function ObserveScreen() {
  return (
    <PlaceholderScreen
      title="Observe"
      body="Camera + observation submission lands in Phase 6."
    />
  );
}
